import React from 'react'

const Tag = (tag) => {
    return (

        <div>
            {console.log("notes Tag")}
            {console.log(tag.tag)}
            {tag.tag}</div>
    )
}

export default Tag